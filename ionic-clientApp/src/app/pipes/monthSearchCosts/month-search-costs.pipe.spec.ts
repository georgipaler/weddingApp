import { MonthSearchCostsPipe } from './month-search-costs.pipe';

describe('MonthSearchCostsPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthSearchCostsPipe();
    expect(pipe).toBeTruthy();
  });
});
